{
  "name": "bun-todo-app",
  "version": "1.0.0",
  "description": "A modern todo app using ElysiaJS, Bun, React, and WASM",
  "type": "module",
  "workspaces": ["backend", "frontend", "wasm"],
  "scripts": {
    "dev": "bun run --filter backend dev & bun run --filter frontend dev",
    "dev:backend": "bun run --filter backend dev",
    "dev:frontend": "bun run --filter frontend dev",
    "build:wasm": "bun run --filter wasm build",
    "build": "bun run build:wasm && bun run --filter backend build && bun run --filter frontend build",
    "start": "bun run --filter backend start"
  },
  "devDependencies": {
    "bun-types": "^1.3.2"
  }
}
