{
  "name": "bun-todo-app",
  "version": "1.0.0",
  "description": "A modern todo app using ElysiaJS, Bun, React, and WASM",
  "type": "module",
  "scripts": {
    "dev": "cd backend && bun run dev & cd frontend && bun run dev & wait",
    "dev:backend": "cd backend && bun run dev",
    "dev:frontend": "cd frontend && bun run dev",
    "build:wasm": "cd wasm && bun run build",
    "build:frontend": "cd frontend && bun run build",
    "build": "bun run build:wasm && bun run build:frontend",
    "start": "cd backend && bun run start"
  },
  "devDependencies": {
    "bun-types": "^1.3.2"
  }
}
